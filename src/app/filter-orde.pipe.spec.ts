import { FilterOrdePipe } from './filter-orde.pipe';

describe('FilterOrdePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOrdePipe();
    expect(pipe).toBeTruthy();
  });
});
